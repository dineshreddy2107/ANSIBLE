- name: nginx install and run
  hosts: frontend
  tasks:
  - name: install nginx server
    ansible.builtin.package:
        name: nginx
        state: present

  - name: start nginx
    anssible.builtin.service:
      name: nginx
      state: started
      enabled: yes

